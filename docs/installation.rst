============
Installation
============

At the command line::

    $ easy_install django-dedal

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-dedal
    $ pip install django-dedal
